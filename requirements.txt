# SignalScope: AI Image Detection & Forensic Studio
# Production and evaluation dependencies

torch>=2.0.0
torchvision>=0.15.0
transformers>=4.30.0
huggingface-hub>=0.16.0
Pillow>=9.5.0
numpy>=1.24.0
scipy>=1.10.0

# Optional: Accelerated ONNX Runtime inference for exported edge checkpoints
onnxruntime>=1.15.0; sys_platform != "darwin" or platform_machine != "arm64"

# Optional: ROC-AUC and confusion matrix evaluation suite
scikit-learn>=1.2.0
